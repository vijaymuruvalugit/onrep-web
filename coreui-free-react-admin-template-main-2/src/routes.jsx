/**
 * Application routes — OnRep role-scoped paths (placeholders until feature migration).
 */

export { protectedRoutes as routes } from './routes/protectedRoutes'
export { default } from './routes/protectedRoutes'
