/**
 * Cross-client activity roadmap (documentation only).
 *
 * Parents, mobile (ezyplay-frontend), notifications, deep links, and analytics should eventually
 * resolve the same operational workspace semantics as OnRep web: explicit partition, no silent
 * fallback, compatible with future activity-prefixed URLs or ?activity= query strategy.
 */

export const CROSS_CLIENT_ACTIVITY_NOTE = 'sync-workspace-semantics-across-web-mobile-parent'
